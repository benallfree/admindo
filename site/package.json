{
  "name": "admindo-site",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "deploy": "bun run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.3.1",
    "admindo": "workspace:*",
    "admindo-plugin-about": "workspace:*",
    "admindo-plugin-dofs": "workspace:*",
    "hono": "^4.7.11",
    "vite": "^6.3.5",
    "vite-plugin-unpkg": "^0.0.4"
  },
  "dependencies": {
    "dofs": "^0.0.1"
  }
}
