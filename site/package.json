{
  "name": "admindo-site",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "deploy": "bun run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.2.2",
    "hono": "^4.7.8",
    "vite": "^6.3.5",
    "admindo": "workspace:*",
    "admindo-plugin-about": "workspace:*",
    "vite-plugin-unpkg": "^0.0.2"
  },
  "dependencies": {
    "dofs": "^0.0.1"
  }
}
